#include <windows.h>
#include "menu.h"

int WINAPI wWinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPWSTR lpCmdLine, int nCmdShow) {
    IniciarMenu(hInstance, nCmdShow);
    return 0;
}
